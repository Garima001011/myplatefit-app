# === PostgreSQL DB Config ===
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/nutrition_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:nutrition_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:nutrition_pass}
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA Settings ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === Thymeleaf Settings ===
spring.thymeleaf.cache=false

# === Server Port (Optional) ===
server.port=8080

# === USDA API Key ===
usda.api.key=${USDA_API_KEY:UvKA15LAdbmAjUz2itwEIk1yOHzjZAvywxcPM9SX}

# === Email SMTP Config ===
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:bhandarirahul112@gmail.com}
spring.mail.password=${MAIL_PASSWORD:bvozwoxyjapvvpik}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spoonacular.api.key=97c184d66bab4711b17f5a743f73f0d4
